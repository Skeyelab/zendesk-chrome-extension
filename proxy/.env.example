# ======================================================================
# Zendesk Chrome Extension Proxy - Environment Configuration
# ======================================================================
# See ../OAUTH_SETUP.md for complete setup instructions
# Quick checklist: ../OAUTH_SETUP_CHECKLIST.md
# ======================================================================

# Server Configuration
PORT=3000
NODE_ENV=development

# Zendesk OAuth Configuration
# Get these from Zendesk Admin Center → APIs → OAuth Clients
# See OAUTH_SETUP.md Step 1 for detailed instructions
ZENDESK_CLIENT_ID=your_client_id_here
ZENDESK_CLIENT_SECRET=your_client_secret_here
ZENDESK_SUBDOMAIN=your_subdomain

# Session Configuration
# Generate a secure random secret: openssl rand -hex 32
# See OAUTH_SETUP.md Step 4.2 for instructions
SESSION_SECRET=your_session_secret_here

# CORS Configuration (comma-separated origins)
# Use your Chrome extension ID from chrome://extensions/
# Format: chrome-extension://<extension-id>
# Run: node ../scripts/get-extension-id.js to get this value
# See OAUTH_SETUP.md Step 2 for how to find extension ID
ALLOWED_ORIGINS=chrome-extension://your-extension-id

# Session Store (memory or redis)
SESSION_STORE=memory

# Redis Configuration (if SESSION_STORE=redis)
# REDIS_URL=redis://localhost:6379
